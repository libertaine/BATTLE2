# pytest.ini
[pytest]
addopts = -q -m "not gui"
pythonpath =
    _legacy
testpaths =
    _legacy/tests
    engine/tests
    client/tests
# Display-backed tests are excluded above and exercised explicitly by the
# dedicated X11 workflow, including the Designer test under root tests/.
norecursedirs =
    dist
    build
    runs
    .venv
markers =
    gui: optional display-backed Linux GUI smoke test
