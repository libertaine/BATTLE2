# pytest.ini
[pytest]
addopts = -q
pythonpath =
    _legacy
testpaths =
    _legacy/tests
    engine/tests
    client/tests
    sdk/tests
norecursedirs =
    dist
    build
    runs
    .venv
markers =
    gui: optional display-backed Linux GUI smoke test
